export global OnboardingGlobal {
    in property <bool> overlay-visible;
    in property <int> current-step;
    in property <int> step-count;
    in property <string> step-title;
    in property <string> step-body;
    in property <string> back-label;
    in property <string> next-label;
    in property <string> skip-label;
    in property <string> finish-label;
}
