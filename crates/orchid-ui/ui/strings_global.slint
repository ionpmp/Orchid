export global Strings {
    in property <string> window-title;
    in property <string> welcome;
    in property <string> subtitle;
    in property <string> version-label;
    in property <string> theme-label;
    in property <string> language-label;
    in property <string> density-label;
    in property <string> get-started-label;
    in property <string> workspace-new-label;
    in property <string> catalog-title;
    in property <string> catalog-search-placeholder;
    in property <string> widget-close-tooltip;
    in property <string> viewer-text-dirty-indicator;
    in property <string> recent-files-open-hint;
    in property <string> rss-open-item-hint;
    in property <string> search-open-hint;

    in property <string> terminal-tooltip-split-h;
    in property <string> terminal-tooltip-split-v;
    in property <string> terminal-tooltip-tab-new;
    in property <string> terminal-tooltip-tab-close;
    in property <string> terminal-tooltip-pane-close;
    in property <string> group-tooltip-dissolve;
    in property <string> group-tooltip-move-left;
    in property <string> group-tooltip-move-right;
    in property <string> group-tooltip-close-tab;
    in property <string> group-hint-alt-detach;

    in property <string> viewer-image-zoom-in;
    in property <string> viewer-image-zoom-out;
    in property <string> viewer-image-rotate-cw;
    in property <string> viewer-image-rotate-ccw;
    in property <string> viewer-image-flip-h;
    in property <string> viewer-image-flip-v;
    in property <string> viewer-pdf-prev-page;
    in property <string> viewer-pdf-next-page;
    in property <string> fm-nav-back;
    in property <string> fm-nav-forward;
    in property <string> fm-nav-up;
    in property <string> fm-nav-home;
    in property <string> fm-loading;
    in property <string> fm-empty-folder;
    in property <string> fm-action-new-folder;
    in property <string> fm-action-new-tab;
    in property <string> fm-action-close-tab;
    in property <string> fm-quick-filter-placeholder;
    in property <string> fm-view-icons;
    in property <string> fm-view-list;
    in property <string> fm-view-details;
    in property <string> fm-view-gallery;
    in property <string> fm-entry-encrypted-hint;
    in property <string> fm-entry-managed-hint;

    in property <string> settings_panel_ok;
    in property <string> settings_open_in_editor;
    in property <string> settings_open_config_file;

    in property <string> media_play;
    in property <string> media_pause;
    in property <string> media_next;
    in property <string> media_previous;

    in property <string> password_locked;
    in property <string> password_no_entries;
    in property <string> password_search_placeholder;
    in property <string> password_select_entry;
    in property <string> password_label_username;
    in property <string> password_label_password;
    in property <string> password_label_url;
    in property <string> password_label_notes;
    in property <string> password_label_totp;
    in property <string> password_copy_username;
    in property <string> password_copy_password;
    in property <string> password_copy_totp;
    in property <string> password_open_url;
    in property <string> password_action_lock;
    in property <string> password_unlock_label;
    in property <string> password_unlock_placeholder;
    in property <string> password_unlock_submit;
    in property <string> password_unlock_biometric;
    in property <string> password_action_add;
}
